# jenkins-terraform
Created Terraform Infra to deploy Jenkins Server on AWS. Terraform Code is in modular farmate. The instance and assigned a elastic IP and have scheduled ebs snapshots once per day.  Jenkins  deployed using docker compose, ensure data persistence for jenkins.
